'''
Các hàm quan trọng trong xử lí chuỗi của python
'''

'''
len(chuoi): (Hàm tích hợp) Trả về độ dài (số lượng ký tự) của chuỗi.

chuoi.lower(): Trả về một bản sao của chuỗi, chuyển đổi tất cả ký tự thành chữ thường.

chuoi.upper(): Trả về một bản sao của chuỗi, chuyển đổi tất cả ký tự thành chữ IN HOA.

chuoi.strip(): Trả về một bản sao của chuỗi, loại bỏ tất cả các khoảng trắng (hoặc ký tự chỉ định) ở đầu và cuối chuỗi.

chuoi.replace(cu, moi): Trả về một bản sao của chuỗi, thay thế tất cả các chuỗi con cu bằng chuỗi moi.

chuoi.split(ky_tu_chia): Tách chuỗi thành một danh sách (list) các chuỗi con, dựa trên ky_tu_chia. (Nếu để trống, mặc định là tách bằng khoảng trắng).

ky_tu_noi.join(danh_sach): Nối các phần tử trong một danh_sach (list) lại thành một chuỗi duy nhất, với ky_tu_noi ở giữa chúng.

chuoi.find(chuoi_con): Tìm vị trí (index) xuất hiện đầu tiên của chuoi_con. Trả về -1 nếu không tìm thấy.

chuoi.isdigit(): Trả về True nếu tất cả ký tự trong chuỗi là chữ số, ngược lại trả về False.

chuoi.isalpha(): Trả về True nếu tất cả ký tự trong chuỗi là chữ cái, ngược lại trả về False.

'''